# Cleaned DSA Site

This package has been cleaned and prepared as a student-friendly static site.

## Added

- Professional light-blue theme in `assets/css/theme.css`
- Shared `header.html` and `footer.html`
- JavaScript include system in `assets/js/include.js`
- Navigation generated from detected HTML file paths in `assets/data/site-map.json`
- Search box in the header
- Breadcrumbs on every updated HTML page
- `sitemap.html`, `sitemap.xml`, and `robots.txt`
- Cleaner root home page
- Removed `.git`, `.vscode`, old duplicate index, and temporary text/script files

## Pages detected

64 public HTML pages are listed in the generated site map. Backend reference templates are kept as source files but are not added to public navigation.

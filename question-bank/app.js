const input=document.querySelector("#search"),cards=[...document.querySelectorAll(".card")],count=document.querySelector("#count");if(input)input.addEventListener("input",()=>{const q=input.value.trim().toLowerCase();let n=0;for(const card of cards){const shown=card.dataset.name.includes(q);card.hidden=!shown;if(shown)n++}count.textContent=`${n} topic${n===1?"":"s"}`});const buttons=[...document.querySelectorAll("[data-setlang]")];function setLanguage(lang){document.body.dataset.lang=lang;document.documentElement.lang=lang==="hi"?"hi":"en";for(const b of buttons){const active=b.dataset.setlang===lang;b.classList.toggle("active",active);b.setAttribute("aria-pressed",String(active))}try{localStorage.setItem("lwc-question-language",lang)}catch{}}if(buttons.length){let saved="both";try{saved=localStorage.getItem("lwc-question-language")||"both"}catch{}setLanguage(["en","hi","both"].includes(saved)?saved:"both");buttons.forEach(b=>b.addEventListener("click",()=>setLanguage(b.dataset.setlang)))}
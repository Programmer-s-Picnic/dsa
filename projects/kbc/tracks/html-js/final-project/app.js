let questions=[],current=0,score=0,selected=null,time=30,timer=null,used={fifty:false,poll:false,skip:false};const ladder=[1000,2000,5000,10000,20000,40000,80000,160000,320000,640000];const qEl=document.getElementById('question'),optionsEl=document.getElementById('options'),msg=document.getElementById('message'),timerEl=document.getElementById('timer'),scoreEl=document.getElementById('score'),ladderEl=document.getElementById('ladder');fetch('questions.json').then(r=>r.json()).then(data=>{questions=data;renderLadder();showQuestion();});function renderLadder(){ladderEl.innerHTML='';ladder.slice().reverse().forEach((p,idx)=>{let real=ladder.length-1-idx;let li=document.createElement('li');li.textContent='₹'+p.toLocaleString('en-IN');if(real===current)li.className='active-prize';ladderEl.appendChild(li);});}function startTimer(){clearInterval(timer);time=30;timerEl.textContent='Time: '+time;timer=setInterval(()=>{time--;timerEl.textContent='Time: '+time;if(time<=0){clearInterval(timer);checkAnswer(null);}},1000);}function showQuestion(){selected=null;msg.textContent='';renderLadder();startTimer();let q=questions[current];qEl.textContent=(current+1)+'. '+q.question;optionsEl.innerHTML='';q.options.forEach(opt=>{let b=document.createElement('button');b.className='option';b.textContent=opt;b.onclick=()=>selectOption(b,opt);optionsEl.appendChild(b);});scoreEl.textContent='Score: '+score;}function selectOption(btn,opt){if(selected)return;selected=opt;checkAnswer(btn);}function checkAnswer(btn){clearInterval(timer);let correct=questions[current].answer;document.querySelectorAll('.option').forEach(b=>{b.disabled=true;if(b.textContent===correct)b.classList.add('correct');});if(selected===correct){score++;msg.textContent='Correct answer!'}else{if(btn)btn.classList.add('wrong');msg.textContent='Wrong. Correct answer: '+correct;}scoreEl.textContent='Score: '+score;}function nextQuestion(){current++;if(current>=questions.length){document.querySelector('.stage').innerHTML=`<h1>Game Over</h1><p>Score: ${score}/${questions.length}</p><p>Prize Won: ₹${(ladder[score-1]||0).toLocaleString('en-IN')}</p><button onclick="restart()">Restart</button>`;return;}showQuestion();}function restart(){location.reload();}function fifty(){if(used.fifty)return;used.fifty=true;let correct=questions[current].answer;let wrong=[...document.querySelectorAll('.option')].filter(b=>b.textContent!==correct);wrong.slice(0,2).forEach(b=>b.style.display='none');}function poll(){if(used.poll)return;used.poll=true;alert('Audience thinks: '+questions[current].answer);}function skip(){if(used.skip)return;used.skip=true;nextQuestion();}document.getElementById('nextBtn').onclick=nextQuestion;document.getElementById('restartBtn').onclick=restart;document.getElementById('fiftyBtn').onclick=fifty;document.getElementById('pollBtn').onclick=poll;document.getElementById('skipBtn').onclick=skip;
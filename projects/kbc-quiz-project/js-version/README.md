# KBC Quiz - HTML/JavaScript Version

## Run

Use VS Code Live Server, or run:

```bash
python -m http.server 8000
```

Then open:

```text
http://localhost:8000
```

Do not open `index.html` directly by double-clicking, because `fetch()` may not load JSON from local files.
